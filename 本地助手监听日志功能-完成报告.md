# 本地助手监听日志功能 - 完成报告

## 完成时间
2026-07-29

## 功能概述
为本地助手添加了详细的监听日志功能，用户可以实时查看：
1. 什么时候刷新监听任务
2. 监听到的文件信息（所有文件、已处理文件、新增文件）
3. 上传结果和错误信息

---

## ✅ 已完成的修改

### 1. 本地助手代码 (local-helper.js)

**文件路径**: `local-helper\local-helper.js`

#### 1.1 添加日志系统配置
```javascript
const LOG_PATH = path.join(__dirname, 'local-helper.log.json');
const MAX_LOG_ENTRIES = 100;  // 最多保留100条日志
let logEntries = [];  // 内存中的日志条目
```

#### 1.2 日志记录函数
新增了以下函数：
- `addLog(type, message, details)` - 添加日志条目
- `loadLogs()` - 启动时加载历史日志

**日志类型**:
- `scan` - 扫描监听任务
- `upload` - 文件上传成功
- `error` - 错误信息
- `info` - 一般信息

#### 1.3 核心监听函数增强
在 `watchAndUpload` 函数中添加了详细的日志记录：

**扫描开始日志**:
```javascript
addLog('scan', `开始扫描监听任务`, {
  folderPath: folderPath || '未设置',
  projectId: projectId,
  time: new Date().toLocaleString('zh-CN')
});
```

**扫描结果日志**:
```javascript
addLog('scan', '扫描完成', {
  folderPath: folderPath,
  totalFiles: localFiles.length,        // 文件夹中所有图片
  processedFiles: processedFiles.size,  // 已处理的图片
  newFiles: newFiles.length,            // 新增的图片
  localFilesList: localFiles.slice(0, 10),  // 文件列表示例
  newFilesList: newFiles.slice(0, 10)       // 新增文件列表示例
});
```

**上传成功日志**:
```javascript
addLog('upload', `上传成功: ${fileName}`, { projectId: projectId });
```

**错误日志**:
```javascript
addLog('error', `上传失败: ${fileName}`, { error: e.message });
addLog('error', `文件夹不存在: ${folderPath}`);
addLog('error', `文件过大，跳过: ${fileName}`, { size: sizeInMB + 'MB' });
```

#### 1.4 HTTP 接口
新增 `/logs` 接口，用于查询监听日志：
```javascript
// GET http://127.0.0.1:9999/logs
// 返回：{ code: 200, data: [...日志条目...] }
```

#### 1.5 主循环增强
在启动时加载历史日志，并记录启动信息：
```javascript
loadLogs();
addLog('info', '本地助手启动', { version: '4.0-local-only' });
```

---

### 2. UI 界面 (helper-ui.ps1)

**文件路径**: `local-helper\helper-ui.ps1`

#### 2.1 新增函数
**`Show-MonitorLogs`** - 显示监听日志窗口
- 从本地助手获取日志数据
- 创建独立的日志查看窗口
- 使用 RichTextBox 显示彩色日志
- 支持刷新功能

#### 2.2 日志显示特性
- **窗口大小**: 900x600 像素，可调整大小
- **字体**: Consolas 9 号（等宽字体）
- **颜色编码**:
  - 🔍 蓝色 - 扫描日志
  - ✅ 绿色 - 上传成功
  - ❌ 红色 - 错误信息
  - ℹ️ 灰色 - 一般信息
- **时间戳**: `[yyyy-MM-dd HH:mm:ss]` 格式
- **详情显示**: JSON 格式显示详细信息

#### 2.3 主界面按钮布局调整
原来：
```
[打开网页配置自动监听]  [检查助手状态................]
```

修改后：
```
[打开网页配置自动监听]  [检查助手状态]  [查看监听日志]
```

#### 2.4 状态检查增强
在 `Show-HelperStatus` 函数中添加了第5项检查：
```
5️⃣  监听状态
    状态: ✅ 正在监听 / ⏸️ 未监听 / ❌ 文件夹不存在
    说明: 文件夹路径和项目ID
```

---

## 📊 日志示例

### 日志文件格式 (local-helper.log.json)
```json
[
  {
    "timestamp": "2026-07-29T10:30:00.000Z",
    "type": "info",
    "message": "本地助手启动",
    "details": { "version": "4.0-local-only" }
  },
  {
    "timestamp": "2026-07-29T10:30:05.000Z",
    "type": "scan",
    "message": "开始扫描监听任务",
    "details": {
      "folderPath": "D:\\Screenshots",
      "projectId": 123,
      "time": "2026-7-29 18:30:05"
    }
  },
  {
    "timestamp": "2026-07-29T10:30:06.000Z",
    "type": "scan",
    "message": "扫描完成",
    "details": {
      "folderPath": "D:\\Screenshots",
      "totalFiles": 50,
      "processedFiles": 45,
      "newFiles": 5,
      "localFilesList": ["战报001.png", "战报002.png", ...],
      "newFilesList": ["战报046.png", "战报047.png", ...]
    }
  },
  {
    "timestamp": "2026-07-29T10:30:07.000Z",
    "type": "upload",
    "message": "上传成功: 战报046.png",
    "details": { "projectId": 123 }
  },
  {
    "timestamp": "2026-07-29T10:30:08.000Z",
    "type": "error",
    "message": "上传失败: 战报047.png",
    "details": { "error": "Network timeout" }
  }
]
```

### 日志窗口显示示例
```
监听日志（最近 100 条）                                [刷新]

[2026-07-29 18:30:00] ℹ️ 本地助手启动
    详情: {"version":"4.0-local-only"}

[2026-07-29 18:30:05] 🔍 开始扫描监听任务
    详情: {"folderPath":"D:\\Screenshots","projectId":123,"time":"2026-7-29 18:30:05"}

[2026-07-29 18:30:06] 🔍 扫描完成
    详情: {"folderPath":"D:\\Screenshots","totalFiles":50,"processedFiles":45,"newFiles":5,...}

[2026-07-29 18:30:07] ✅ 上传成功: 战报046.png
    详情: {"projectId":123}

[2026-07-29 18:30:08] ❌ 上传失败: 战报047.png
    详情: {"error":"Network timeout"}
```

---

## 🔄 监听机制说明

### 问题1: 什么时候刷新监听任务？
**答案**: 每隔 **5 秒** 执行一次扫描

监听流程：
```
启动本地助手
    ↓
进入主循环 (while true)
    ↓
[日志] 开始扫描监听任务
    ↓
检查文件夹是否存在
    ↓
查询数据库中已处理的文件
    ↓
扫描本地文件夹
    ↓
计算新增文件 (本地文件 - 已处理文件)
    ↓
[日志] 扫描完成 (记录文件数量)
    ↓
逐个上传新增文件
    ↓
[日志] 上传成功/失败
    ↓
等待 5 秒
    ↓
回到主循环开始 (持续监听)
```

### 问题2: 如何实现一直监听不中断？
**答案**: 使用 `while(true)` 无限循环 + `setTimeout` 间隔

**核心代码**:
```javascript
// 主循环：监听文件夹 + 上传文件
while (true) {
  try {
    // 监听文件夹并上传新文件
    await watchAndUpload(config);
  } catch (e) {
    console.error('❌ 监听失败:', e.message);
    addLog('error', '监听失败', { error: e.message });
  }

  // 等待下一轮（5秒）
  await new Promise(resolve => setTimeout(resolve, POLL_INTERVAL * 1000));
}
```

**特点**:
- ✅ 无限循环，永不停止
- ✅ 每轮之间等待 5 秒
- ✅ 即使某轮出错，也会继续下一轮
- ✅ 只要进程不退出，就会一直监听

---

## 📝 日志记录的信息

### 1. 扫描日志 (每5秒一次)
记录内容：
- **开始扫描**: 文件夹路径、项目ID、时间
- **扫描结果**: 
  - 文件夹中所有图片数量
  - 已处理的图片数量
  - 新增的图片数量
  - 所有图片的文件名列表（前10个）
  - 新增图片的文件名列表（前10个）

### 2. 上传日志
记录内容：
- 上传成功的文件名
- 上传失败的文件名和错误原因
- 项目ID

### 3. 错误日志
记录内容：
- 文件夹不存在
- 文件过大被跳过
- 查询数据库失败
- 上传失败的具体原因

### 4. 信息日志
记录内容：
- 本地助手启动
- 文件夹恢复访问
- 配置变更

---

## 🎯 使用方法

### 查看日志的3种方式

#### 方式1: 通过UI界面（推荐）
1. 打开本地助手窗口
2. 点击"查看监听日志"按钮
3. 在弹出的窗口中查看彩色日志
4. 点击"刷新"按钮获取最新日志

#### 方式2: 通过HTTP接口
```bash
# 使用浏览器或curl
curl http://127.0.0.1:9999/logs
```

返回格式：
```json
{
  "code": 200,
  "data": [
    {
      "timestamp": "2026-07-29T10:30:00.000Z",
      "type": "scan",
      "message": "开始扫描监听任务",
      "details": { ... }
    }
  ]
}
```

#### 方式3: 直接查看日志文件
文件位置：`local-helper\local-helper.log.json`

可以用任何文本编辑器打开，或使用 JSON 格式化工具查看。

---

## 💡 监听频率说明

### 当前配置: 5秒/次
```javascript
const POLL_INTERVAL = 5;  // 轮询间隔（秒）
```

### 如何修改监听频率？

如果觉得 5 秒太慢，可以修改 `local-helper.js` 中的 `POLL_INTERVAL` 常量：

```javascript
// 更频繁的监听
const POLL_INTERVAL = 2;  // 2秒/次

// 更节省资源的监听
const POLL_INTERVAL = 10;  // 10秒/次
```

**建议值**:
- **2-3秒**: 需要快速响应新文件
- **5秒**: 平衡性能和及时性（推荐）
- **10秒**: 节省系统资源

---

## 🔍 故障排查

### 问题1: 看不到日志
**可能原因**:
- 本地助手未运行
- 本地助手版本过旧

**解决方案**:
1. 检查本地助手是否运行
2. 重启本地助手
3. 确认使用的是 v4.0 版本

### 问题2: 日志显示"未配置监听文件夹"
**原因**: 尚未在网页中选择监听文件夹

**解决方案**:
1. 在网页中登录账号
2. 选择项目
3. 点击"选择监听文件夹"
4. 选择要监听的文件夹

### 问题3: 日志显示"文件夹不存在"
**原因**: 配置的文件夹路径不存在或已被删除

**解决方案**:
1. 检查文件夹是否存在
2. 重新在网页中选择正确的文件夹

---

## 📦 相关文件

### 修改的文件
- `local-helper\local-helper.js` - 本地助手主程序
- `local-helper\helper-ui.ps1` - UI界面程序

### 新增的文件
- `local-helper\local-helper.log.json` - 日志文件（运行时自动生成）

### 文档文件
- `本地助手v4.0-重构说明.md` - 设计文档
- `本地助手v4.0-清理完成报告.md` - 清理报告
- `本地助手监听日志功能-完成报告.md` - 本报告

---

## 🎉 总结

本次更新成功为本地助手添加了完整的监听日志功能：

✅ **日志记录完善**: 记录所有关键操作和状态变化
✅ **UI界面友好**: 彩色日志、清晰分类、易于阅读
✅ **实时刷新**: 点击按钮即可查看最新日志
✅ **监听机制透明**: 用户清楚知道何时扫描、扫描了什么、上传了什么
✅ **故障诊断简单**: 通过日志快速定位问题

现在用户可以清楚地看到：
1. ✅ 每 5 秒扫描一次监听任务
2. ✅ 文件夹中有多少图片
3. ✅ 已经处理过多少图片
4. ✅ 新增了多少图片需要上传
5. ✅ 哪些文件上传成功，哪些失败
