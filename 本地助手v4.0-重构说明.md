# 本地助手 v4.0 重构说明

## 核心设计变更

### 旧设计（v3.0）的问题
- 文件夹路径存储在后端数据库
- 本地助手从后端获取任务列表和文件夹路径
- 路径变更需要经过后端同步
- 复杂的 taskFolders 映射关系

### 新设计（v4.0）
- **文件夹路径完全是本地的事情，与后端无关**
- 前端选择文件夹 → 直接通知本地助手
- 本地助手立即开始监听新路径
- 后端只负责接收上传的图片，不存储路径

## 已完成的修改

### 1. 本地助手代码 ✅
- 创建了新版本：`local-helper\local-helper-v4.js`
- 已替换到：`local-helper\local-helper.js`
- 备份旧版本：`local-helper\local-helper-v3-backup.js`

**核心变更**：
- 移除了从后端获取任务列表的逻辑（`listTasks`）
- 简化配置文件结构为：`{ folderPath, projectId }`
- 新增 `/set-folder` 接口，前端直接设置监听文件夹
- 移除了 `taskFolders` 的复杂映射
- 移除了 `/bind-task` 接口

### 2. 前端代码 ✅
- 修改了 `ocr-watch-v2.js` 中的 `saveOcrWatchFolder` 函数
- 将 `/bind-task` 调用改为 `/set-folder`
- 移除了向后端保存 folderPath 的逻辑

## 需要进一步调整的部分

### 1. 后端接口清理 ⚠️

后端仍然有一些过时的接口和逻辑需要清理：

#### 需要移除或简化的接口：
- `GET /api/ocr-watch/tasks` - 不再需要返回 `folderPath`
- `POST /api/ocr-watch/tasks` - 不再需要保存 `folderPath` 到数据库
- `POST /api/ocr-watch/tasks/:id/folder-status` - 不再需要（目录状态是本地的）
- `POST /api/ocr-watch/tasks/:id/heartbeat` - 可能不再需要（取决于是否还需要任务管理）

#### 数据库表 `helper_configs` 的字段：
- `folder_path` - 可以废弃
- `folder_status` - 可以废弃
- `folder_status_message` - 可以废弃

### 2. 上传接口调整 ⚠️

本地助手上传文件时的请求体：
```javascript
// 当前的上传请求（local-helper-v4.js:196-199）
{
  image: base64,
  projectId: projectId || null,
  imageName: fileName,
  source: 'auto-watch'
  // 已移除 helperTaskId 字段
}
```

后端接收时不应该再期望 `helperTaskId` 字段。

## 设计对比

| 项目 | v3.0（旧） | v4.0（新） |
|------|-----------|-----------|
| 文件夹路径来源 | 后端数据库 | 本地配置文件 |
| 路径变更方式 | 前端→后端→本地助手 | 前端→本地助手 |
| 配置文件结构 | `taskFolders: {taskId: path}` | `folderPath: "path"` |
| 后端是否存储路径 | ✅ 存储 | ❌ 不存储 |
| 任务系统 | 支持多任务 | 单文件夹监听 |
| 前端接口 | `/bind-task` | `/set-folder` |

## 使用流程

### v4.0 新流程
```
1. 用户在前端选择项目
2. 点击"选择监听文件夹"
3. 本地助手弹出文件夹选择对话框
4. 用户选择文件夹
5. 前端调用 POST http://127.0.0.1:9999/set-folder
   参数：{ folderPath, projectId }
6. 本地助手立即更新配置，下个轮询周期（最多5秒）开始监听
7. 发现新图片 → 上传到后端 /api/battles/ocr-tasks
```

## 前端接口调用示例

```javascript
// 设置监听文件夹
async function setWatchFolder(folderPath, projectId) {
  const response = await fetch('http://127.0.0.1:9999/set-folder', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      folderPath: folderPath,
      projectId: projectId || 0
    })
  });
  
  const result = await response.json();
  if (result.code === 200) {
    console.log('✅ 监听文件夹已设置:', result.folderPath);
  }
}

// 查询本地助手状态
async function getHelperStatus() {
  const response = await fetch('http://127.0.0.1:9999/status');
  const result = await response.json();
  
  console.log('当前监听:', result.folderPath);
  console.log('项目ID:', result.projectId);
  console.log('文件夹是否存在:', result.folderExists);
}
```

## 配置文件示例

### local-helper.config.json
```json
{
  "apiBase": "https://api.zhenwu.fun/api",
  "helperToken": "helper-auth-xxx",
  "helperClientId": "xxx",
  "deviceId": "device-xxx",
  "folderPath": "D:\\Screenshots",
  "projectId": 123
}
```

## 下一步行动

### 必要的清理工作：
1. **清理后端接口** - 移除 `folderPath` 相关的数据库操作
2. **清理数据库字段** - 标记 `folder_path` 等字段为废弃
3. **测试新流程** - 确保文件夹选择和监听功能正常

### 可选的优化：
1. 添加多文件夹监听支持（如果需要）
2. 减少轮询间隔（从5秒改为更短）
3. 添加 WebSocket 实时通知（替代轮询）

## 回滚方案

如果新版本有问题，可以快速回滚：

```bash
cd local-helper
cp local-helper-v3-backup.js local-helper.js
```

前端代码也需要恢复 `saveOcrWatchFolder` 函数中的旧逻辑。
