# 修复说明：本地助手离线显示问题

## 问题描述

网页端的本地助手监听状态，隔一段时间就显示"⚠️ 离线"，即使本地助手实际在正常运行。

## 问题根因

1. **前端判断逻辑**：如果心跳时间（`lastHeartbeat`）超过 2 分钟（120秒），就显示"离线"
2. **后端更新心跳时机**：只在以下3个时刻更新心跳
   - 任务初始化
   - 文件上传后刷新统计
   - 任务状态控制（启动/暂停/停止）
3. **本地助手行为**：每5秒轮询一次，但**不主动发送心跳**（代码注释：`3. 不做心跳检测（过度设计）`）

**结果**：如果本地助手运行中但没有新文件上传，超过2分钟后前端就会判定为"离线"。

## 解决方案

### 1. 后端添加心跳接口

**文件**：`nslg-backend.js`

**位置**：第 2267 行之后

**新增接口**：
```javascript
// 4.6. 本地助手心跳更新
app.post('/api/ocr-watch/tasks/:id/heartbeat', requireOcrUploadActor, async (req, res) => {
  try {
    const taskId = Number(req.params.id);

    if (!Number.isInteger(taskId) || taskId <= 0) {
      return res.json({ code: 400, message: '任务参数无效' });
    }

    // 只更新心跳时间，不重新统计数据库
    const state = watchTaskStates.get(taskId);
    if (state) {
      state.lastHeartbeat = new Date();
      broadcastTaskUpdate(taskId);
    }

    res.json({ code: 200, message: '心跳已更新' });
  } catch (err) {
    console.error('[OCR-Watch] 更新心跳失败:', err);
    res.json({ code: 500, message: err.message });
  }
});
```

**特点**：
- 轻量级：只更新心跳时间，不统计数据库
- 无需传递参数：只需要 taskId
- 通过 WebSocket 实时广播更新

### 2. 本地助手定期发送心跳

**文件**：`local-helper/local-helper.js`

**修改1**：添加心跳发送函数（第113行之后）

```javascript
// ========== 核心功能1.6：发送心跳 ==========

async function sendHeartbeat(config, taskId) {
  try {
    await apiFetch(config, `/ocr-watch/tasks/${taskId}/heartbeat`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' }
    });
  } catch (e) {
    // 心跳失败不影响主流程，仅记录日志
    console.warn(`[心跳] 任务 ${taskId} 心跳发送失败:`, e.message);
  }
}
```

**修改2**：在主循环中发送心跳（第558行）

```javascript
// 处理所有 running 状态的任务
for (const task of tasks) {
  if (task.status === 'running') {
    // 发送心跳保持在线状态
    await sendHeartbeat(config, task.id);

    // 处理任务文件
    await processTask(config, task);
  }
}
```

**逻辑**：
- 每5秒轮询一次任务列表
- 对于每个 `running` 状态的任务，先发送心跳，再处理文件
- 心跳失败不影响文件处理流程

## 验证方法

### 1. 启动监听任务

1. 访问 https://www.zhenwu.fun
2. 进入任意项目，点击"自动监听战报解析"
3. 点击"开始监听"按钮
4. 状态应该显示：**✅ 运行中**

### 2. 等待超过2分钟

**之前的问题**：
- 如果没有新文件上传，2分钟后会显示：**⚠️ 离线**

**修复后的行为**：
- 即使没有新文件，也会持续显示：**✅ 运行中**
- 因为每5秒发送一次心跳，前端判断心跳时间永远不会超过2分钟

### 3. 查看浏览器控制台日志

打开浏览器控制台（F12），应该能看到类似日志：

```
[OCR-Watch] UI update: status=running effective=running heartbeatAge=3256ms lastHeartbeat=2026-07-24T08:52:15.123Z
```

关键点：`heartbeatAge` 应该始终小于 10000ms（10秒），不会超过 120000ms（2分钟）

### 4. 检查 WebSocket 推送

控制台应该每5秒左右看到：

```
[WebSocket] 收到任务更新: {taskId: 1, status: "running", lastHeartbeat: "..."}
```

## 技术细节

### 心跳频率
- **本地助手轮询间隔**：5秒
- **前端离线判断阈值**：120秒（2分钟）
- **安全余量**：24倍（120秒 / 5秒 = 24）

### 性能影响
- **每次心跳请求**：轻量级 POST 请求，只更新内存中的时间戳
- **不触发数据库查询**：与统计接口分离
- **WebSocket 推送**：复用现有广播机制，无额外开销

### 容错设计
- 心跳失败不影响文件处理
- 只在 `running` 状态时发送心跳
- 心跳失败只记录日志，不抛出异常

## 部署信息

- **提交哈希**：`f6d042a`
- **提交信息**：修复本地助手离线显示问题：添加心跳机制保持在线状态
- **修改文件**：
  - `nslg-backend.js` - 后端心跳接口
  - `local-helper/local-helper.js` - 本地助手心跳发送
- **服务重启时间**：2026-07-24 16:49

## 相关问题

### 如果心跳接口失败怎么办？

心跳失败不会影响核心功能：
1. 文件上传仍然正常工作
2. 文件上传后会触发 `refreshOcrWatchTaskStats`，间接更新心跳
3. 前端只是会暂时显示"离线"，但不影响实际监听

### 为什么不用轮询接口替代？

虽然 `/api/ocr-watch/tasks` 接口也能获取任务列表，但：
1. 本地助手已经在调用这个接口获取任务列表
2. 这个接口不会更新后端内存中的 `lastHeartbeat`
3. 需要专门的心跳更新机制来通知后端"我还在线"

### 为什么不在文件上传时更新心跳？

文件上传时**已经会更新心跳**（通过 `refreshOcrWatchTaskStats`），但问题是：
- 如果没有新文件，就不会触发上传
- 如果没有上传，就不会更新心跳
- 如果不更新心跳，超过2分钟就显示离线

所以需要**独立的心跳机制**，不依赖文件上传。
